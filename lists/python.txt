vim $DOTFILES_DIR/lists/python.txt                                      # EDIT
sh $DOTFILES_DIR/scripts/python/create-environment.sh                   # CREATE ENVIRONMENT
source venv/bin/activate                                                # ACTIVATE
python -m venv venv                                                     # CREATE VENV
streamlit run chef.py --server.port=8080 --server.address=0.0.0.0 --server.enableCORS=false --server.enableXsrfProtection=false # STREAMLIT
pip3 freeze > requirements.txt                                          # SAVE DEPENDENCIES
pip3 install -r requirements.txt                                        # INSTALL DEPENDENCIES
source .env                                                             # SOURCE ENV
source .env && source venv/bin/activate && python main.py               # START
~/Developer/blog && python scripts/compose.py                           # COMPOSE BLOG
python ~/Developer/blog/scripts/create_sitemap.py                       # CREATE SITEMAP
source venv/bin/activate                                                # ACTIVATE
deactivate                                                              # DEACTIVATE
coverage run -m unittest discover -s app/tests -p "test_*.py"           # RUN TEST CASES
coverage report                                                         # CREATE COVERAGE REPORT
coverage html                                                           
python -m pip install --upgrade pip setuptools wheel                    # UPGRADE PACKAGING TOOLS
