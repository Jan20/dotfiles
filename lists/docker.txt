vim -c 'set filetype=sh' ~/Developer/tools/terminal-setup/lists/docker.txt			# EDIT
docker images											# IMAGES
docker stop $(docker ps -q) && docker rm $(docker ps -a -q)					# REMOVE CONTAINERS
docker compose down										# COMPOSE DOWN
docker volume ls										# LIST VOLUMES
docker volume prune										# PRUNE
docker ps											# LIST CONTAINERS
docker-compose up -d										# COMPOSE UP
osascript -e 'quit app "Docker"'								# CLOSE DOCKER
